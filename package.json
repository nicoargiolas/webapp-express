{
  "name": "webapp-express",
  "version": "1.0.0",
  "description": "Backend per la web app",
  "main": "app.js",
  "scripts": {
    "start": "node --env-file=.env app.js",
    "watch": "node --env-file=.env --watch app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "movies",
    "webapp",
    "backend"
  ],
  "author": "Nicola Argiolas",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2",
    "mysql2": "^3.13.0"
  }
}